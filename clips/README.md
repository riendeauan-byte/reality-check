# clips/

Put your transparent WebM clips here (VP9 with alpha). One is picked at random
each time the overlay fires.

Do not commit clips you do not own. See "Add clips" in the main README for the
`import_clip.sh` and `prep/` tools that generate them.
